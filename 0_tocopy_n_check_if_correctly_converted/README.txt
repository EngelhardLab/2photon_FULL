check_raw.m > to check if all sessions in a raw folder (or any folder) that are found in the converted folder are correctly converted

check_sizes.py > to check if both (original and copy) have the same size (aka the copy was successful)

GROUP_ROBOCOPY.py > to robocopy multiple sessions (needs listforrobocopy.txt) 

robocopy_only_ch2_files.txt > contains the command to run a robocopy of only Ch2 (from terminal) - works only for one session

test_2p_file_conversion.m > to check if the conversion from RAW files into TIF files was done correctly - works only for one session